#############################################
# NASA Research Assistant - Environment Vars #
#############################################

# Frontend (Create React App)
# Avoid exposing secrets in the browser in production. For local dev only.
REACT_APP_GEMINI_API_KEY=
REACT_APP_BACKEND_URL=

# Backend (Flask)
GEMINI_API_KEY=

# CORS (comma separated) e.g., http://localhost:3000,https://myapp.com
ALLOWED_ORIGINS=*

# Port for Flask when running locally
PORT=5000